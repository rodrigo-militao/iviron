<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Inviron Camera Login</title>
</head>
<body>

    <center>
        <div class="">
            <img src="assets/img/logo.png" class="img-logo" width="210" height="70">
        </div>

        <h1>Login</h1>
        <form method="POST" action="/login">
            <input type="text" id="username" name="username" placeholder="Usuário"  />
            <input type="password" id="password" name="password" placeholder="Senha"  />
    
            <button id="btn_logar">
                Logar
            </button>
        </form>

    </center>


    <!-- javascript files-->
    <script src="assets/js/jquery.js"></script>
    <script>
        $(document).keypress(function(event) {
            if (event.keyCode === 13) {
                $("#btn_logar").click();
            }
        })
        
        /* $("#btn_logar").on('click', function() {
            //Fazer fetch para saber se usuario e senha estão corretos.
            //Se estiverem, vai chamar o método login para passar pra próxima tela.
            const usuarioCerto = {user: "rodrigo", password: "123"}

            const user = $("#username").val()
            const password = $("#password").val()

            const usuarioSendoLogado = {
                user, 
                password,
            }

            if(usuarioSendoLogado.user == usuarioCerto.user && usuarioSendoLogado.password == usuarioCerto.password) {
                window.global.login({user: "rodrigo"})
            } else {
                alert('Login ou senha incorretos.')
            }
        }) */
    </script>
    
</body>
</html>